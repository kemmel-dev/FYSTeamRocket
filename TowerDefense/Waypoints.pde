/**
 * Holds the coordinates of the waypoints.
 * these floats represent either an X or an Y coordinate.
 * @author Kamiel de Visser | 500838438
 */
class Waypoints
{
    float tileSize = SIZE_X / grid.NUM_TILES_X;
    
    float wayPoint1 = tileSize * 3.5;
    float wayPoint2 = tileSize * 2.5;
    float wayPoint3 = tileSize * 6.5;
    float wayPoint4 = tileSize * 7.5;
    float wayPoint5 = tileSize * 10.5;
    float wayPoint6 = tileSize * 3.5;
}